# ha-emon-config
A Home Assistant Custom Integration (HACS) that provides a web-based configuration interface for OpenEnergyMonitor devices (emonTx, emonPi). Works with an ESPHome-based ESP32 serial bridge to send configuration commands and receive serial data from your energy monitoring devices.
